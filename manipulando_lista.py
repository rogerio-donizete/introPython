#lista em python tem uma estrutura de pilha


#lista.append -> insere um item
# lista.claer -> limpa listam
# lista.copy -> replica a lista (é um outro objeto)
# lista.sort -> ordena a lista em ordm cecente
# lista.sort(recerse=True) -> ordena a lista emordem descreente
# lista.sort(key=lambda x: len(x)) -> ordena a lista por tamanho dos valores
# lista.sort(key=lambda x: len(x), recerse=True) -> ordena a lista por tamanho dos  em orde descreente
# lista.sort() -> ordena a lista
# lista.count -> conta valores iguais numa lista.
# lista.extend[listaAlvo, lista2] -> adiciona os valores da lsita 2 na lista alvo
# lista.index[]valor x-> mostra o indide da priemira ocorrencia do valor
# lista.pop -> remove o ultimo item da lista
# lista.pop(indice) -> remove  o valor do indice passado
# lista.remove(valor) -> removeo valor passado
# lista.reverse -> inverte a ordem da lista
# lista.len -> informa o tamanho da lista
# lista.sorte -> faz a mesma funçaõ da sort, mas é built in
